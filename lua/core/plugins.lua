-- plugins.lua
-- Plugin management with lazy.nvim

-- This file is not used directly for plugin specifications
-- It just ensures the plugin system is properly set up
-- Actual plugin specs are in the lua/plugins/ directory

return {
  -- Plugin specs will be loaded from the 'plugins' directory
  -- Each file in that directory should return a table of plugin specs
} 